# 🔌 Подключение к сервису «Сбор данных с сайтов» (MCP / Kilo Code)

> **Что это.** Готовый инструмент для сбора информации с любого сайта прямо из
> Kilo Code: агент открывает страницу, а если сайт закрыт антиботом (капча,
> «Проверяем браузер», Cloudflare) — автоматически использует наш антидетект-стенд
> и возвращает нужные данные.

---

## 🎯 Что вы получаете

- **Простое подключение** — 10 минут, без VPN и сложных настроек.
- **Обход антиботов** — маркетплейсы (Ozon, Wildberries, Avito и др.), форумы,
  каталоги — всё доступно для парсинга.
- **Два режима:**
  - `scrape_markdown` — обычные сайты, быстро (Firecrawl);
  - `scrape_url` — «тяжёлые» и защищённые сайты (антидетект-воркер).

---

## 🧱 Как это устроено

```
            Kilo Code (у вас)
                 │
    ┌────────────┴─────────────┐
    │  MCP-сервер firecrawl    │  ← скрипт mcp/firecrawl_mcp.py
    └────────────┬─────────────┘
                 ▼
   ┌───────────────────────────┐
   │   Стенд (общий IP)        │
   │  • Firecrawl    :3002     │  обычные сайты
   │  • Camoufox-воркер :3100  │  антибот-сайты
   └───────────────────────────┘
```

Ваш компьютер не грузится, всё работает на стенде.

---

## 📋 Что понадобится

| Требование | Как проверить |
|---|---|
| Установленный **Kilo Code** | — |
| **Python 3** | `python3 --version` |
| Доступ к стенду по сети | адреса ниже, публичные |

Адреса стенда (выдаются внутреннему пользователю, токен — у администратора):
- воркер: `http://109.94.1.210:3100/scrape`
- Firecrawl: `http://109.94.1.210:3002`

---

## ⚙️ Пошаговая настройка

### Шаг 1. Положите скрипт
Скопируйте файл `mcp/firecrawl_mcp.py` в удобное место:
```bash
mkdir -p ~/mcp
cp mcp/firecrawl_mcp.py ~/mcp/firecrawl_mcp.py
```

### Шаг 2. Добавьте MCP-сервер в конфиг Kilo
Откройте `~/.config/kilo/kilo.jsonc` (или проектный `kilo.jsonc`) и вставьте:

```jsonc
{
  "mcp": {
    "firecrawl-stack": {
      "type": "local",
      "command": ["python3", "/полный/путь/firecrawl_mcp.py"],
      "environment": {
        "WORKER_URL": "http://109.94.1.210:3100/scrape",
        "FIRECRAWL_URL": "http://109.94.1.210:3002",
        "AUTH_TOKEN": "НОВЫЙ-ТОКЕН-ОТ-АДМИНИСТРАТОРА"
      },
      "enabled": true
    }
  }
}
```

> ⚠️ В поле `command` — **абсолютный путь**, `~/mcp/...` не подойдёт.

### Шаг 3. Установите скилл (чтобы агент работал сам)
Скопируйте папку скилла в свой проект:
```bash
mkdir -p <ваш_проект>/.kilo/skill
cp -r skill/firecrawl-stack <ваш_проект>/.kilo/skill/
```
Если работаете прямо в этом репозитории — скилл уже лежит в `.kilo/skill/`, копировать не надо.

### Шаг 4. Активируйте
1. Перезапустите Kilo или откройте `/mcps`.
2. Убедитесь, что сервер `firecrawl-stack` активен.
3. В диалоге должны появиться инструменты: `scrape_url`, `scrape_markdown`.

---

## ✅ Как это работает у вас (сценарий)

1. Вы пишете: **«Собери данные с Wildberries о термометрах»**.
2. Агент открывает сайт, но там антибот.
3. По скиллу он вызывает `scrape_url` → стенд возвращает данные.
4. Вы получаете структурированный ответ: товары, цены, скидки, рейтинги.

Пример результата (реальный):

| Товар | Цена | Скидка | Рейтинг | Отзывы |
|---|--:|--:|--:|--:|
| Термометр WT-03 электронный (B.Well) | 331 ₽ | 35% | 4.9 | 1283 |
| Термометр Little Doctor LD-300 | 259 ₽ | 35% | 4.9 | 1370 |
| Гигрометр-термометр комнатный (ELLWOOD) | 190 ₽ | 63% | 4.9 | 47 |

---

## 🛠️ Подсказки

- **`scrape_markdown`** — используйте для обычных сайтов (новости, статьи, блоги).
- **`scrape_url`** — для маркетплейсов и «защищённых» страниц.
- **Капча — не финал.** Если агент наткнулся на «Проверяем браузер»,
  Cloudflare или 403, он сам переключится на стенд (`scrape_url`/`scrape_cloak`).
  Считайте «не смог» только после того, как стенд вернул `captcha`/`ip-blocked`.
- Если сайт вернул `captcha` или `ip-blocked` — повторите запрос позже; возможно,
  потребуется чистый доступ на стороне стенда.

---

## 🔐 Важно знать

- Токен **общий** — не передавайте его за пределы команды. При утечке
  администратор меняет его, старый перестаёт работать.
- Стенд — один браузер, **запросы идут по очереди**. Между запросами делайте
  паузу несколько секунд.
- Один запрос занимает ~5–30 секунд — не плодите дублирующих запросов.

---

## ❓ FAQ

**Я ничего не вижу после добавления конфига.**
Перезапустите Kilo или вызовите `/mcps`, проверьте, что сервер отмечен как активный.

**Воркер отвечает 401.**
Неверный или не выдан `AUTH_TOKEN`. Запросите токен у администратора.

**Контент пришёл, но без цен.**
Уточните у администратора формат выдачи для этого сайта; для рынков часто нужно
обращаться к API, а не к HTML.

---

*Возникли вопросы? Администратор: @achizhov · [telegram] · [почта]*
